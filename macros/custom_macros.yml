version: 2

macros:
  - name: unix_nano_to_timestamp
    desctiption: "function to convert Uniz nano secs to BigQuery Timestamp datatype"
    arguments:
      - name: column_name
        type: string
        description: "The name of a column"
  
  - name: generate_schema_name
    desctiption: "Custom macro to get schema name"
    arguments:
      - name: custom_schema_name
        type: string
        description: "The name of a schema"
      - name: node
        type: 
        description: "node"
